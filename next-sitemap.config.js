/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.zenith-web.co.uk/', // kendi domainin
  generateRobotsTxt: true,              // robots.txt de otomatik oluşur
  changefreq: 'daily',
  priority: 0.7,
  sitemapSize: 5000,
}
